# ✨ BREAK REMINDER FEATURE

## 🎯 ЧТО ЭТО ДЕЛАЕТ

Компонент `BreakReminder.js` напоминает пользователю каждый час отдохнуть:

```
✅ Напоминание каждый час (3600 секунд)
✅ Звуковой сигнал (генерируется через Web Audio API)
✅ Браузерное уведомление (если разрешено)
✅ Красивое всплывающее окно в приложении
✅ Кнопка "Взять перерыв" (таймер на 5 минут)
✅ Кнопка "Закрыть" 
✅ Включение/отключение через кнопку в углу экрана
✅ Сохранение состояния в localStorage
```

---

## 🎨 ГДЕ НАХОДИТСЯ

**Кнопка включения:**
```
📍 Правый нижний угол экрана
   🔔 кнопка (зеленая = включено, серая = отключено)
   ✓ ON / ✗ OFF
```

**Уведомление:**
```
📍 Верхний правый угол (когда пришло напоминание)
   - Фиолетовый градиент
   - Плавная анимация появления
   - Автоматически исчезает через 10 сек
```

---

## 🔧 КАК ЭТО РАБОТАЕТ

### 1. Включение/Отключение
```javascript
// Пользователь нажимает зеленую кнопку
toggleReminder() → isEnabled = true/false

// Состояние сохраняется в localStorage
localStorage.setItem('breakReminderEnabled', true)

// При перезагрузке состояние восстанавливается
JSON.parse(localStorage.getItem('breakReminderEnabled'))
```

### 2. Таймер каждый час
```javascript
// Каждый час (3600000 мс = 1 час)
setInterval(() => {
  setShowNotification(true)  // Показать уведомление
  playNotificationSound()     // Проиграть звук
  sendBrowserNotification()   // Браузерное уведомление
  
  // Автоскрытие через 10 сек
  setTimeout(() => setShowNotification(false), 10000)
}, 3600000)
```

### 3. Звуковой сигнал
```javascript
// Используется Web Audio API (без необходимости загружать файл)
const audioContext = new AudioContext()
const oscillator = audioContext.createOscillator()
oscillator.frequency.value = 800  // 800 Hz (приятный звук)
// Воспроизводится 0.5 сек
```

### 4. Браузерное уведомление
```javascript
// Если пользователь разрешил уведомления
new Notification('Time for a break! ☕', {
  body: 'You have been working for an hour...',
  requireInteraction: true  // Не исчезает пока не кликнуть
})
```

### 5. Быстрый перерыв (5 минут)
```javascript
// Кнопка "Take Break"
setInterval(() => {
  alert('5 minutes are up!')
}, 5 * 60 * 1000)  // 5 минут = 300000 мс
```

---

## 📱 КАК ПОЛЬЗОВАТЕЛЬ ВИДИТ

### Обычное состояние (включено)
```
                     ┌─────────┐
                     │ 💡 Tip: │
                     │ You'll  │
                     │ get a   │
                     │ remind. │
                     └─────────┘
                     
                     ┌─────┐
                     │ 🔔  │
                     │✓ ON │
                     └─────┘
```

### Когда пришло напоминание (каждый час)
```
┌─────────────────────────────────────┐
│ ☕ Time for a Break!               │ X
│ You've been working for 1 hour...  │
│ ┌──────────────────────────────┐   │
│ │ ✓ Take Break  │ X            │   │
│ └──────────────────────────────┘   │
│ Last reminder: 14:32:45            │
└─────────────────────────────────────┘
```

---

## 🚀 ВКЛЮЧЕНИЕ ФУНКЦИИ

1. **Откройте приложение**
   ```
   http://localhost:3000
   ```

2. **Авторизуйтесь**
   ```
   Введите username и пароль
   ```

3. **Найдите кнопку в углу**
   ```
   Правый нижний угол экрана
   Серая кнопка 🔔
   ```

4. **Нажмите кнопку**
   ```
   🔔 → 🔔 (станет зеленой)
   ✗ OFF → ✓ ON
   
   Возможно браузер попросит разрешение на уведомления
   Нажмите "Allow"
   ```

5. **Готово!**
   ```
   Каждый час будет приходить напоминание
   ```

---

## ⚙️ ТЕХНИЧЕСКИЕ ДЕТАЛИ

### Компонент: `BreakReminder.js`

**State:**
```javascript
isEnabled        // Включена ли функция (true/false)
showNotification // Показывать ли уведомление
lastReminderTime // Время последнего напоминания
```

**Functions:**
```javascript
toggleReminder()              // Включить/отключить
playNotificationSound()       // Звуковой сигнал
requestNotificationPermission() // Запрос разрешения браузера
dismissNotification()         // Закрыть уведомление
takeQuickBreak()             // Таймер 5 минут
```

**Storage:**
```javascript
localStorage.setItem('breakReminderEnabled', true/false)
// Состояние сохраняется и восстанавливается при перезагрузке
```

---

## 🔔 БРАУЗЕРНЫЕ УВЕДОМЛЕНИЯ

### Первый запуск
```
Браузер попросит: "Разрешить уведомления?"
Выберите: Allow / Block
```

### Если разрешили
```
Уведомление будет:
- Показано даже если вкладка не активна
- Содержит звук (если включены в системе)
- Требует клика для закрытия
```

### Если блокировали
```
Функция все еще работает:
- Звуковой сигнал будет
- Всплывающее окно в приложении будет
- Только браузерное уведомление не будет
```

---

## 📋 ФАЙЛЫ

| Файл | Что изменилось |
|------|-----------------|
| `client/src/components/BreakReminder.js` | ✅ СОЗДАН (285 строк) |
| `client/src/App.js` | ✅ ОБНОВЛЕН (импорт + добавлен компонент) |

---

## 🎯 ПРИМЕР ИСПОЛЬЗОВАНИЯ

```javascript
// В App.js
import BreakReminder from './components/BreakReminder';

function AppContent() {
  const { user } = useAuth();
  
  return (
    <>
      {user && <Navbar />}
      {user && <BreakReminder />}  {/* ← ДОБАВЛЕНО */}
      <Routes>
        {/* ... */}
      </Routes>
    </>
  );
}
```

---

## 🧪 ТЕСТИРОВАНИЕ

### Быстрый тест (без ожидания 1 часа)

Отредактируйте в `BreakReminder.js` строку:

```javascript
// ДО (1 час):
}, 3600000);  // 1 час

// ПОСЛЕ (30 секунд для теста):
}, 30000);    // 30 секунд для теста!
```

Потом вернуть обратно.

---

## 💡 ФУНКЦИИ

### Базовые
- ✅ Напоминание каждый час
- ✅ Включение/отключение
- ✅ Звуковой сигнал
- ✅ Браузерное уведомление
- ✅ Всплывающее окно

### Продвинутые
- ✅ Сохранение в localStorage
- ✅ Таймер перерыва (5 минут)
- ✅ Красивая анимация
- ✅ Гибкие настройки
- ✅ Работает в фоне

---

## 🚀 БУДУЩИЕ УЛУЧШЕНИЯ

```
□ Настройка интервала (30 мин, 1 час, 2 часа)
□ Выбор типа уведомления (звук/вибрация/молчание)
□ Статистика (сколько перерывов за день)
□ Выключение напоминаний в определенное время
□ Список предложенных упражнений
□ Интеграция с уведомлениями в приложении
□ Темы для уведомлений (разные стили)
```

---

## ❓ FAQ

**Q: Может ли функция срабатывать если приложение закрыто?**  
A: Нет, только если вкладка открыта. Для фонового сервиса нужен Service Worker.

**Q: Что если я не разрешу браузерные уведомления?**  
A: Звуковой сигнал и всплывающее окно все еще будут работать.

**Q: Состояние сохраняется если я перезагружу?**  
A: Да! Используется localStorage, поэтому если вы включили функцию, она останется включена.

**Q: Можно ли изменить интервал?**  
A: Да! Отредактируйте число `3600000` в строке `setInterval(..., 3600000)`

**Q: Работает ли на мобильных?**  
A: Да! Браузерные уведомления работают на мобильных лучше, чем на десктопе.

---

**Автор:** AI Assistant  
**Версия:** 1.0.0  
**Статус:** ✅ ГОТОВО  
**Дата:** 27.01.2026
