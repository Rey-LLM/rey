# 🚀 ИНСТРУКЦИЯ ПО ЗАПУСКУ ПРОЕКТА

## ШАГИ ДЛЯ ЗАПУСКА

### 1️⃣ ПОДГОТОВКА (выполни ОДИН РАЗ)

```bash
# Перейди в папку проекта
cd /workspaces/rey

# Установи backend зависимости
npm install

# Установи frontend зависимости
cd client
npm install
cd ..
```

### 2️⃣ ЗАПУСК MONGODB (обязательно первым делом!)

Открой **новый терминал** и выполни:
```bash
mongod
```

Оставь этот терминал открытым! Он должен показать:
```
[initandlisten] Waiting for connections on port 27017
```

### 3️⃣ ЗАПУСК BACKEND СЕРВЕРА

Открой **второй терминал** и выполни:
```bash
cd /workspaces/rey
npm run dev
```

Должно появиться:
```
🚀 Server running on http://localhost:5000
📊 WebSocket server active
```

### 4️⃣ ЗАПУСК FRONTEND (React)

Открой **третий терминал** и выполни:
```bash
cd /workspaces/rey/client
npm start
```

Автоматически откроется браузер на http://localhost:3000

---

## ✅ ВСЁ РАБОТАЕТ, ЕСЛИ:

- [ ] MongoDB запущена (терминал 1)
- [ ] Backend работает на http://localhost:5000/api/health (терминал 2)
- [ ] React приложение открылось на http://localhost:3000 (терминал 3)

---

## 🧪 ТЕСТИРОВАНИЕ

### 1. Перейди на http://localhost:3000

### 2. Нажми "Register" и создай аккаунт:
- Username: `testuser`
- Email: `test@example.com`
- Password: `password123`

### 3. Нажми "Sign In" с теми же данными

### 4. Если всё работает → увидишь "ProjectsList" страницу

---

## 🔍 ПРОВЕРКА ОШИБОК

### Если видишь ошибку подключения к MongoDB:
```
✗ MongoDB connection error
```
**Решение:** Убедись, что `mongod` запущен в первом терминале

### Если видишь ошибку CORS:
```
Access to XMLHttpRequest blocked by CORS policy
```
**Решение:** Проверь .env файл - должна быть строка:
```
CLIENT_URL=http://localhost:3000
```

### Если видишь ошибку портов:
```
EADDRINUSE: address already in use :::5000
```
**Решение:** Порт 5000 занят. Убей процесс:
```bash
# На Linux/Mac
lsof -ti:5000 | xargs kill -9

# Или измени PORT в .env файле на 5001
```

---

## 📂 ВСЕ ФАЙЛЫ НАХОДЯТСЯ В:

```
/workspaces/rey/
├── .env ← ВАЖНО! Переменные окружения
├── server.js ← Backend
├── package.json ← Backend зависимости
├── routes/ ← API маршруты
├── models/ ← Базы данных схемы
├── middleware/ ← Проверка аутентификации
└── client/ ← React приложение
    ├── src/
    │   ├── App.js
    │   ├── AuthContext.js
    │   ├── services.js
    │   ├── components/
    │   ├── pages/
    │   └── api.js
    └── public/
        └── index.html
```

---

## 🎯 БЫСТРЫЙ СТАРТ (одна строка)

Если всё уже установлено, просто выполни в 3 разных терминалах:

**Терминал 1:**
```bash
mongod
```

**Терминал 2:**
```bash
cd /workspaces/rey && npm run dev
```

**Терминал 3:**
```bash
cd /workspaces/rey/client && npm start
```

---

## 💾 КУДА СОХРАНЕНЫ ВСЕ ФАЙЛЫ:

✅ **Всё в `/workspaces/rey/`** - это твоя рабочая папка

Если захочешь скачать на свой компьютер:
```bash
# Архивируй всю папку
cd /workspaces
tar -czf rey-project.tar.gz rey/

# Или используй git
cd /workspaces/rey
git init
git add .
git commit -m "Initial commit"
```

---

## 🚨 ВАЖНО!

1. **ВСЕГДА** сначала запускай MongoDB (`mongod`)
2. **ПОТОМ** запускай Backend (`npm run dev`)
3. **ПОТОМ** запускай Frontend (`npm start`)

Порядок важен!

---

## 📝 Файл .env (ТЫ УЖЕ СОЗДАЛ)

```
MONGODB_URI=mongodb://localhost:27017/project-manager
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production
PORT=5000
CLIENT_URL=http://localhost:3000
NODE_ENV=development
```

---

## ❓ ПРОБЛЕМЫ?

Если что-то не работает:

1. Проверь что запущено в каждом терминале
2. Посмотри логи ошибок в консоли
3. Убедись что используешь Node.js 16+: `node --version`
4. Перезагрузи: закрой все 3 терминала и начни заново

---

**Успехи! 🎉**
