# 🚀 REY PROJECT - ПОЛНЫЙ ИНДЕКС

## 📍 МЕСТОПОЛОЖЕНИЕ
```
/workspaces/rey/
```

---

## 📋 ДОКУМЕНТАЦИЯ

| Файл | Назначение | Время чтения |
|------|-----------|------------|
| **00_НАЧНИ_ОТСЮДА.txt** | ⭐ Шпаргалка с 3 командами | **1 мин** |
| **PROJECT_MAP.txt** | Карта проекта и структура | **2 мин** |
| **QUICK_START.md** | Быстрый старт | **2 мин** |
| **START_HERE.md** | Полный путеводитель | **5 мин** |
| **INSTALLATION_GUIDE.md** | Подробная инструкция | **10 мин** |
| **API_EXAMPLES.md** | Примеры API запросов | **5 мин** |
| **CODE_REVIEW.md** | Проверка кода (готово) | **5 мин** |
| **FINAL_SUMMARY.md** | Финальная сводка | **3 мин** |
| **README.md** | Основное описание | **2 мин** |

---

## ⚡ САМЫЙ БЫСТРЫЙ ЗАПУСК

### Вариант 1: Три команды в трёх терминалах

```bash
# ТЕРМИНАЛ 1
mongod

# ТЕРМИНАЛ 2
cd /workspaces/rey && npm run dev

# ТЕРМИНАЛ 3
cd /workspaces/rey/client && npm start
```

**Результат:** http://localhost:3000 откроется автоматически

### Вариант 2: Если MongoDB уже запущена

```bash
# ТЕРМИНАЛ 1
cd /workspaces/rey && npm run dev

# ТЕРМИНАЛ 2
cd /workspaces/rey/client && npm start
```

---

## 🛠️ ВСЕ ПОЛЕЗНЫЕ КОМАНДЫ

### Backend команды
```bash
cd /workspaces/rey

npm run dev      # Разработка (с autoreload)
npm start        # Production запуск
npm install      # Установить зависимости
```

### Frontend команды
```bash
cd /workspaces/rey/client

npm start        # Разработка (с hot reload)
npm run build    # Production сборка
npm test         # Запустить тесты
npm install      # Установить зависимости
```

### MongoDB
```bash
mongod           # Запустить сервер
```

### Проверка окружения
```bash
bash /workspaces/rey/check-environment.sh
```

### Убить процесс на порту
```bash
# Порт 5000 (Backend)
lsof -ti:5000 | xargs kill -9

# Порт 3000 (Frontend)
lsof -ti:3000 | xargs kill -9

# Порт 27017 (MongoDB)
lsof -ti:27017 | xargs kill -9
```

---

## 📂 ФАЙЛЫ ПРОЕКТА

### Документация и конфигурация
```
.env                      ← Переменные окружения (СОЗДАН ✅)
.env.example             ← Пример .env файла
.gitignore               ← Git ignore правила
README.md                ← Основное описание
package.json             ← Backend зависимости
check-environment.sh     ← Проверка окружения
```

### Backend файлы
```
server.js                ← Express сервер (ГЛАВНЫЙ)
middleware/
  └── auth.js            ← JWT проверка
models/
  ├── User.js            ← Схема пользователя
  ├── Project.js         ← Схема проекта
  └── Task.js            ← Схема задачи
routes/
  ├── auth.js            ← Аутентификация
  ├── projects.js        ← Проекты CRUD
  ├── tasks.js           ← Задачи CRUD
  ├── users.js           ← Пользователи
  └── recommendations.js ← Рекомендации
```

### Frontend файлы
```
client/
  ├── package.json       ← React зависимости
  ├── public/
  │   └── index.html     ← HTML шаблон
  └── src/
      ├── App.js         ← Главное приложение
      ├── AuthContext.js ← Управление аутентификацией
      ├── api.js         ← Axios конфигурация
      ├── services.js    ← API сервисы
      ├── index.js       ← React entry point
      ├── components/
      │   ├── Navbar.js
      │   └── ProjectsList.js
      └── pages/
          ├── Login.js
          ├── Register.js
          ├── ProjectDetail.js
          └── Dashboard.js
```

---

## 🧪 ТЕСТИРОВАНИЕ ПРИЛОЖЕНИЯ

### Шаг 1: Запустить приложение (3 команды выше)

### Шаг 2: Открыть в браузере
```
http://localhost:3000
```

### Шаг 3: Создать тестового пользователя
- Нажми "Register"
- Заполни форму:
  - Username: `testuser`
  - Email: `test@example.com`
  - Password: `password123`

### Шаг 4: Войти в систему
- Нажми "Sign In"
- Используй те же данные

### Шаг 5: Проверить Backend
```bash
curl http://localhost:5000/api/health
```

Ответ должен быть:
```json
{"status":"ok","timestamp":"2026-01-26T..."}
```

---

## 🔧 ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ (.env)

```
# MongoDB подключение
MONGODB_URI=mongodb://localhost:27017/project-manager

# JWT secret (ИЗМЕНИ перед production!)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Порты
PORT=5000
CLIENT_URL=http://localhost:3000

# Окружение
NODE_ENV=development
```

---

## 🌐 API МАРШРУТЫ

### Аутентификация
```
POST   /api/auth/register          - Регистрация
POST   /api/auth/login             - Вход
POST   /api/auth/change-password   - Смена пароля
POST   /api/auth/forgot-password   - Восстановление пароля
```

### Проекты
```
GET    /api/projects               - Все проекты
POST   /api/projects               - Создать
GET    /api/projects/{id}          - Получить
PUT    /api/projects/{id}          - Обновить
DELETE /api/projects/{id}          - Удалить
POST   /api/projects/{id}/members  - Добавить члена
GET    /api/projects/{id}/stats    - Статистика
```

### Задачи
```
GET    /api/tasks                  - Все задачи
POST   /api/tasks                  - Создать
GET    /api/tasks/{id}             - Получить
PUT    /api/tasks/{id}             - Обновить
PATCH  /api/tasks/{id}/status      - Изменить статус
DELETE /api/tasks/{id}             - Удалить
POST   /api/tasks/{id}/comments    - Добавить комментарий
GET    /api/tasks/project/{projId} - Задачи проекта
```

### Пользователи
```
GET    /api/users/me               - Текущий пользователь
GET    /api/users/{id}             - Профиль пользователя
PUT    /api/users/me               - Обновить профиль
PUT    /api/users/me/preferences   - Обновить настройки
GET    /api/users/{id}/activity    - Активность пользователя
```

---

## 🚨 ЧАСТЫЕ ПРОБЛЕМЫ И РЕШЕНИЯ

### ❌ "MongoDB connection error"
```bash
✅ Запусти: mongod
```

### ❌ "Port 5000 already in use"
```bash
✅ Запусти: lsof -ti:5000 | xargs kill -9
   Или измени PORT в .env на 5001
```

### ❌ "Cannot GET /api/projects"
```bash
✅ Убедись что Backend запущен на порту 5000
✅ Проверь что ты залогинен (есть token)
```

### ❌ "CORS error"
```bash
✅ Проверь .env файл
✅ Убедись что CLIENT_URL=http://localhost:3000
```

### ❌ "React компоненты не загружаются"
```bash
cd /workspaces/rey/client
npm cache clean --force
npm install
npm start
```

### ❌ "Invalid token" при входе
```bash
✅ Очистить localStorage: F12 → Application → localStorage → clear
✅ Перезагрузить страницу (Ctrl+R)
```

---

## 📊 СТАТУС ПРОЕКТА

| Компонент | Статус | Примечание |
|-----------|--------|-----------|
| Backend (Express) | ✅ | Готово к запуску |
| Frontend (React) | ✅ | Готово к запуску |
| MongoDB модели | ✅ | User, Project, Task |
| Аутентификация | ✅ | JWT + Context API |
| API маршруты | ✅ | Полные CRUD операции |
| WebSocket | ✅ | Real-time обновления |
| Валидация | ✅ | express-validator |
| Ошибки | ✅ | Всё исправлено |

---

## 💾 СКАЧИВАНИЕ ПРОЕКТА

### Как один архив
```bash
cd /workspaces
tar -czf rey-project.tar.gz rey/

# Распаковать на другом компьютере:
tar -xzf rey-project.tar.gz
cd rey
```

### Через Git
```bash
cd /workspaces/rey
git init
git add .
git commit -m "Initial commit"
git remote add origin https://github.com/yourusername/rey.git
git push origin main
```

---

## 🎯 СЛЕДУЮЩИЕ ШАГИ

1. ✅ Запустить проект (3 команды)
2. 📖 Прочитать [API_EXAMPLES.md](API_EXAMPLES.md)
3. 🔍 Изучить код в `client/src/` и `routes/`
4. 🛠️ Модифицировать по своим нуждам
5. 🚀 Развертывать в production

---

## 📞 БЫСТРЫЕ ССЫЛКИ

- 📄 [00_НАЧНИ_ОТСЮДА.txt](00_НАЧНИ_ОТСЮДА.txt) - Шпаргалка
- 🗺️ [PROJECT_MAP.txt](PROJECT_MAP.txt) - Карта проекта
- ⚡ [QUICK_START.md](QUICK_START.md) - За 2 минуты
- 📖 [START_HERE.md](START_HERE.md) - Полный путеводитель
- 📚 [INSTALLATION_GUIDE.md](INSTALLATION_GUIDE.md) - Инструкция
- 🔌 [API_EXAMPLES.md](API_EXAMPLES.md) - Примеры API
- ✅ [CODE_REVIEW.md](CODE_REVIEW.md) - Проверка кода
- 📝 [FINAL_SUMMARY.md](FINAL_SUMMARY.md) - Финальная сводка

---

## ✨ ЗАКЛЮЧЕНИЕ

**Весь код проверен ✓ и готов к использованию!**

Начни с 3 команд:
```bash
# Терминал 1
mongod

# Терминал 2
cd /workspaces/rey && npm run dev

# Терминал 3
cd /workspaces/rey/client && npm start
```

**Удачи! 🚀**
