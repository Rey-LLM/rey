# БЫСТРАЯ ШПАРГАЛКА

## 📍 ВСЁ НАХОДИТСЯ ЗДЕСЬ:
```
/workspaces/rey/
```

## 🚀 3 КОМАНДЫ ДЛЯ ЗАПУСКА

### В ТЕРМИНАЛЕ 1:
```bash
mongod
```

### В ТЕРМИНАЛЕ 2:
```bash
cd /workspaces/rey && npm run dev
```

### В ТЕРМИНАЛЕ 3:
```bash
cd /workspaces/rey/client && npm start
```

---

## ✅ ГОТОВО?

Если видишь это в браузере → **ВСЁ РАБОТАЕТ**:
- http://localhost:3000 (страница входа)
- http://localhost:5000/api/health (ответ JSON)

---

## 📋 ОСНОВНЫЕ КОМАНДЫ

| Команда | Что делает |
|---------|-----------|
| `npm install` | Установить backend зависимости |
| `cd client && npm install` | Установить frontend зависимости |
| `npm run dev` | Запустить backend в режиме разработки |
| `npm start` (в client/) | Запустить React приложение |
| `npm run build` (в client/) | Собрать production версию React |

---

## 🔑 ЛОГИН ДЛЯ ТЕСТА

После регистрации используй эти данные:
- Email: `test@example.com`
- Password: `password123`

---

## 📂 СТРУКТУРА ПРОЕКТА

```
/workspaces/rey/
├── .env ← ПЕРЕМЕННЫЕ
├── server.js ← ГЛАВНЫЙ BACKEND
├── package.json ← ЗАВИСИМОСТИ
├── middleware/ ← ПРОВЕРКА AUTH
├── models/ ← БД СХЕМЫ
├── routes/ ← API МАРШРУТЫ
└── client/
    ├── src/ ← REACT КОД
    ├── public/ ← HTML
    └── package.json ← REACT ЗАВИСИМОСТИ
```

---

**Читай INSTALLATION_GUIDE.md для полных инструкций!**
