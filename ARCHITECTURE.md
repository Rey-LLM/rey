# 🏗️ АРХИТЕКТУРА ФУНКЦИИ "ВСЕ ДОКУМЕНТЫ"

## 📊 Общая архитектура

```
┌─────────────────────────────────────────────────────────────────┐
│                    REACT FRONTEND (Client)                       │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  App.js (Router Config)                                          │
│  ↓                                                                │
│  Routes:                                                         │
│  ├─ /documents → PrivateRoute → DocumentsList Component         │
│  └─ /projects, /login, etc.                                     │
│                                                                  │
│  DocumentsList.js (Main Component)                              │
│  ├─ State Management                                            │
│  │  ├─ documents: []       - все документы                      │
│  │  ├─ folders: {}         - документы по папкам                │
│  │  ├─ selectedFolder: string - текущая папка                   │
│  │  ├─ sortBy: string      - тип сортировки                     │
│  │  ├─ sortOrder: string   - порядок сортировки                 │
│  │  ├─ searchTerm: string  - текст поиска                       │
│  │  └─ stats: object       - статистика                         │
│  │                                                               │
│  ├─ Effects                                                      │
│  │  └─ useEffect → fetchDocuments()                             │
│  │                                                               │
│  ├─ UI Components                                               │
│  │  ├─ Header (Заголовок и статистика)                         │
│  │  ├─ Controls (Поиск и сортировка)                           │
│  │  ├─ FoldersView (Папки с документами)                       │
│  │  └─ Footer (Общая статистика)                               │
│  │                                                               │
│  └─ Styles                                                      │
│     └─ DocumentsList.css (400+ строк)                          │
│                                                                  │
│  Navbar.js (Navigation)                                         │
│  ├─ Link: /documents → "📚 Documents"                          │
│  └─ + другие навигационные ссылки                              │
│                                                                  │
└────────────┬───────────────────────────────────────────────────┘
             │ HTTP/REST API (axios)
             │ Authorization: Bearer JWT_TOKEN
             │
┌────────────┴───────────────────────────────────────────────────┐
│                    EXPRESS BACKEND (Server)                      │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  server.js (Express Setup)                                      │
│  ├─ Imports:                                                     │
│  │  ├─ express, mongoose, cors, socket.io                       │
│  │  └─ documentRoutes ← NEW                                      │
│  │                                                               │
│  ├─ Routes Registration:                                        │
│  │  ├─ app.use('/api/auth', authRoutes)                         │
│  │  ├─ app.use('/api/projects', projectRoutes)                  │
│  │  ├─ app.use('/api/tasks', taskRoutes)                        │
│  │  ├─ app.use('/api/users', userRoutes)                        │
│  │  └─ app.use('/api/documents', documentRoutes) ← NEW          │
│  │                                                               │
│  ├─ Middleware:                                                 │
│  │  ├─ cors()                                                    │
│  │  ├─ express.json()                                           │
│  │  ├─ express-validator                                        │
│  │  └─ authenticateToken (JWT protection)                       │
│  │                                                               │
│  └─ Error Handling                                              │
│                                                                  │
│  routes/documents.js (NEW - 306 строк)                         │
│  ├─ GET /api/documents                                          │
│  │  ├─ Параметры: sortBy, order, search, category             │
│  │  ├─ Получить проекты пользователя                          │
│  │  ├─ Получить все задачи из проектов                        │
│  │  ├─ Получить все вложения                                   │
│  │  ├─ Сгруппировать по папкам                                 │
│  │  ├─ Применить фильтры и сортировку                          │
│  │  └─ Возвращает: documents[], folders{}, stats{}             │
│  │                                                               │
│  ├─ GET /api/documents/project/:projectId                      │
│  │  ├─ Получить документы конкретного проекта                  │
│  │  ├─ Проверить доступ                                        │
│  │  └─ Вернуть сгруппированные документы                       │
│  │                                                               │
│  └─ GET /api/documents/categories/list                         │
│     ├─ Получить все категории пользователя                     │
│     └─ Вернуть список категорий                                 │
│                                                                  │
│  routes/projects.js (UPDATED)                                  │
│  ├─ PUT /api/projects/:projectId                               │
│  │  ├─ Добавлена валидация:                                    │
│  │  │  ├─ body('name') - 3+ символа                           │
│  │  │  ├─ body('category') - enum validation                   │
│  │  │  ├─ body('status') - enum validation                     │
│  │  │  ├─ body('priority') - enum validation                   │
│  │  │  └─ body('startDate', 'dueDate') - ISO8601               │
│  │  └─ + другие проверки                                        │
│  │                                                               │
│  └─ + другие маршруты (GET, POST, DELETE)                      │
│                                                                  │
│  models/Project.js (MongoDB Schema)                            │
│  ├─ name: String                                               │
│  ├─ owner: ObjectId (User)                                     │
│  ├─ members: [{user, role, joinedAt}]                          │
│  ├─ category: enum                                             │
│  ├─ tasks: [ObjectId] → Task models                           │
│  ├─ attachments: [{name, url, uploadedBy, uploadedAt}]         │
│  └─ + другие поля                                               │
│                                                                  │
│  models/Task.js (MongoDB Schema)                               │
│  ├─ title: String                                              │
│  ├─ project: ObjectId (Project)                                │
│  ├─ status: enum (todo, in-progress, review, done, blocked)    │
│  ├─ priority: enum (low, medium, high, urgent)                 │
│  ├─ category: String                                           │
│  ├─ tags: [String]                                             │
│  └─ + другие поля                                               │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
             │ MongoDB (Database)
             │
┌────────────┴───────────────────────────────────────────────────┐
│                    MONGODB DATABASE                              │
├─────────────────────────────────────────────────────────────────┤
│                                                                  │
│  Collections:                                                    │
│  ├─ users                                                       │
│  ├─ projects                                                    │
│  │  ├─ owner (ref: User)                                       │
│  │  ├─ members (ref: User)                                     │
│  │  ├─ tasks (ref: Task)                                       │
│  │  └─ attachments                                             │
│  │                                                               │
│  └─ tasks                                                       │
│     ├─ project (ref: Project)                                  │
│     ├─ creator (ref: User)                                     │
│     ├─ assignee (ref: User)                                    │
│     ├─ status                                                  │
│     ├─ priority                                                │
│     └─ tags                                                    │
│                                                                  │
└─────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Поток данных

### 1. Загрузка документов
```
Пользователь откроет /documents
    ↓
DocumentsList.js (mount)
    ↓
useEffect с зависимостями [sortBy, sortOrder, searchTerm, selectedFolder]
    ↓
fetchDocuments() - axios GET запрос
    ↓
Authorization: Bearer TOKEN
    ↓
API: GET /api/documents?sortBy=date&order=desc&search=...
    ↓
Backend получает запрос
    ↓
Middleware: authenticateToken (验证 JWT)
    ↓
router.get('/') в documents.js
    ↓
1. Найти все проекты пользователя
2. Получить все задачи из этих проектов
3. Получить все вложения
4. Применить фильтры и сортировку
5. Сгруппировать по папкам
6. Подсчитать статистику
    ↓
Вернуть JSON ответ:
{
  documents: [...],
  folders: {...},
  stats: {...}
}
    ↓
Frontend получает ответ
    ↓
setDocuments(), setFolders(), setStats()
    ↓
React перерисовывает компонент
    ↓
Пользователь видит все документы
```

### 2. Поиск в реальном времени
```
Пользователь вводит текст в поле поиска
    ↓
onChange → setSearchTerm(text)
    ↓
searchTerm изменился
    ↓
useEffect запускается (searchTerm в зависимостях)
    ↓
Создается новый запрос с параметром ?search=text
    ↓
fetchDocuments() вызывается
    ↓
Процесс повторяется (см. выше)
```

### 3. Сортировка
```
Пользователь выбирает сортировку
    ↓
setSortBy(value) или setSortOrder(newOrder)
    ↓
useEffect запускается
    ↓
Новый запрос с параметрами ?sortBy=name&order=asc
    ↓
На сервере: documents сортируются
    ↓
Frontend отображает отсортированные результаты
```

### 4. Группировка по папкам
```
На сервере:
foreach документ:
  folders[документ.category] = []
  folders[документ.category].push(документ)

На фронтенде:
foreach папка в folders:
  <folder-section>
    <folder-header> Название папки (количество)
    <documents-grid> Все документы папки
```

---

## 📁 Структура файлов

```
/workspaces/rey/
├── server.js (UPDATED)
│   └─ Импорт documentRoutes
│   └─ Регистрация /api/documents
│
├── routes/ (UPDATED)
│   ├── documents.js (NEW - 306 строк)
│   │   ├─ GET /api/documents
│   │   ├─ GET /api/documents/project/:projectId
│   │   └─ GET /api/documents/categories/list
│   │
│   └── projects.js (UPDATED)
│       └─ PUT /api/projects/:projectId (добавлена валидация)
│
├── models/
│   ├── Project.js
│   └── Task.js
│
├── middleware/
│   └── auth.js
│
├── client/
│   └── src/
│       ├── App.js (UPDATED)
│       │   └─ Новый маршрут /documents
│       │
│       ├── pages/
│       │   ├── DocumentsList.js (NEW - 280 строк)
│       │   ├── Login.js
│       │   └── ProjectDetail.js
│       │
│       ├── components/
│       │   ├── Navbar.js (UPDATED)
│       │   │   └─ Ссылка на /documents
│       │   └── ProjectsList.js
│       │
│       └── styles/
│           ├── DocumentsList.css (NEW - 400+ строк)
│           └── SearchBar.css
│
├── BUGFIX_REPORT.md (NEW)
├── DOCUMENTS_GUIDE.md (NEW)
├── DOCUMENTS_QUICK_START.md (NEW)
├── FINAL_CHANGES_REPORT.md (NEW)
└── test-documents-api.sh (NEW)
```

---

## 🔐 Безопасность

```
Каждый запрос:
1. Проходит middleware: authenticateToken
   ↓
2. Проверяется JWT токен
   ↓
3. Извлекается req.user.id
   ↓
4. На сервере запрашиваются только:
   - Проекты, где пользователь owner
   - Проекты, где пользователь в members
   ↓
5. Если доступа нет - ошибка 403 Forbidden
```

---

## 💾 Оптимизация БД

```
documents.js:
├─ .lean() - возвращает POJO, не Mongoose документы
├─ .populate() - заполняет ссылки на другие коллекции
├─ Индексы на Project: owner, status, текстовый поиск
└─ Кэширование на клиенте в state

Результат:
- Быстрые запросы
- Низкое использование памяти
- Оптимизация для БД
```

---

## 📊 Статистика

```
В документах.js подсчитываются:

stats = {
  total: количество всех документов,
  
  byCategory: {
    development: 20,
    design: 15,
    marketing: 7
  },
  
  byType: {
    tasks: 40,
    attachments: 2
  },
  
  byStatus: {
    todo: 10,
    in-progress: 15,
    review: 8,
    done: 8,
    blocked: 1
  }
}
```

---

## 🔄 Интеграция с существующей системой

```
Существующие компоненты:
├─ AuthContext - управление авторизацией
├─ Navbar - главная навигация
└─ App.js - маршрутизация

Новая функция интегрируется:
├─ Использует AuthContext для токена
├─ Добавляется в Navbar для навигации
├─ Добавляется маршрут в App.js
└─ Использует существующие модели (Project, Task)
```

---

## 🚀 Масштабируемость

### Текущие возможности:
- Поддержка тысяч документов
- Групповая фильтрация
- Полнотекстовый поиск

### Для увеличения производительности:
- Добавить пагинацию (limit, offset)
- Добавить кэширование (Redis)
- Добавить индексы на часто используемые поля
- Использовать агрегацию MongoDB

---

**Архитектура спроектирована для:**
- ✅ Масштабируемости
- ✅ Безопасности
- ✅ Производительности
- ✅ Удобства разработки
- ✅ Легкой поддержки

