============================================
  🚀 ПРОЕКТ REY - БЫСТРЫЙ ЗАПУСК
============================================

ВСЕ ФАЙЛЫ НАХОДЯТСЯ В: /workspaces/rey/

✅ КОД ГОТОВ К ЗАПУСКУ - БЕЗ ИЗМЕНЕНИЙ!

============================================
📋 ДОКУМЕНТАЦИЯ (ЧИТАЙ В ЭТОМ ПОРЯДКЕ)
============================================

1. START_HERE.md          ← Полный путеводитель
2. QUICK_START.md         ← За 2 минуты (3 команды)
3. INSTALLATION_GUIDE.md  ← Подробная инструкция
4. API_EXAMPLES.md        ← Примеры API запросов
5. CODE_REVIEW.md         ← Проверка кода (готово)
6. FINAL_SUMMARY.md       ← Эта сводка

============================================
🚀 ЗАПУСК В 3 КОМАНДЫ (В РАЗНЫХ ТЕРМИНАЛАХ)
============================================

ТЕРМИНАЛ 1 (MongoDB):
  mongod

ТЕРМИНАЛ 2 (Backend):
  cd /workspaces/rey && npm run dev

ТЕРМИНАЛ 3 (Frontend):
  cd /workspaces/rey/client && npm start

👉 Откроется http://localhost:3000

============================================
✅ ПРОВЕРКА ОКРУЖЕНИЯ
============================================

Перед запуском:

✅ .env файл - СОЗДАН
   - MONGODB_URI=mongodb://localhost:27017/project-manager
   - JWT_SECRET=your-super-secret-jwt-key
   - PORT=5000
   - CLIENT_URL=http://localhost:3000

✅ Node.js - проверить версию
   node --version     (нужно 16+)
   npm --version      (нужно 8+)

✅ MongoDB - установить
   mongod --version
   Если нет: sudo apt-get install mongodb

✅ Зависимости
   npm install            (backend)
   cd client && npm install (frontend)

============================================
🧪 ПЕРВЫЙ ТЕСТ
============================================

1. Запусти в 3 терминалах (как выше)

2. Открой http://localhost:3000

3. Нажми "Register":
   Username: testuser
   Email: test@example.com
   Password: password123

4. Нажми "Sign In"

5. Если видишь список проектов → ВСЁ РАБОТАЕТ! 🎉

============================================
📂 СТРУКТУРА ПРОЕКТА
============================================

/workspaces/rey/
├── server.js          ← Backend главный файл
├── .env               ← Переменные (СОЗДАН ✅)
├── package.json       ← Backend зависимости
├── middleware/        ← JWT проверка
├── models/            ← MongoDB схемы
├── routes/            ← API маршруты
└── client/            ← React приложение

============================================
🔑 ВАЖНО!
============================================

⚠️ Порядок запуска:
  1. MongoDB (mongod)
  2. Backend (npm run dev)
  3. Frontend (npm start)

⚠️ Используй 3 разных терминала!

⚠️ MongoDB должна быть ЗАПУЩЕНА!
  Если забыл - просто запусти mongod в первом терминале

============================================
🔧 КОМАНДЫ
============================================

Backend:
  npm run dev      ← Разработка
  npm start        ← Production

Frontend:
  npm start        ← Разработка
  npm run build    ← Сборка

MongoDB:
  mongod           ← Запуск сервера

============================================
🔌 ПРОВЕРКА РАБОТЫ
============================================

Backend работает?
  curl http://localhost:5000/api/health
  Ответ: {"status":"ok","timestamp":"..."}

Frontend работает?
  http://localhost:3000
  Должна открыться страница входа

MongoDB работает?
  mongod должна вывести: "Waiting for connections"

============================================
🐛 ЧАСТЫЕ ПРОБЛЕМЫ
============================================

❌ "MongoDB connection error"
✅ Запусти: mongod

❌ "Port 5000 already in use"
✅ Запусти: lsof -ti:5000 | xargs kill -9
   Или измени PORT в .env

❌ "CORS ошибка"
✅ Проверь .env: CLIENT_URL=http://localhost:3000

❌ "React компоненты не загружаются"
✅ npm cache clean --force
   cd client && npm install

============================================
📚 API МАРШРУТЫ
============================================

Регистрация:
  POST /api/auth/register

Вход:
  POST /api/auth/login

Проекты:
  GET /api/projects
  POST /api/projects
  GET /api/projects/{id}
  PUT /api/projects/{id}
  DELETE /api/projects/{id}

Задачи:
  GET /api/tasks
  POST /api/tasks
  PATCH /api/tasks/{id}/status
  POST /api/tasks/{id}/comments

Пользователь:
  GET /api/users/me
  PUT /api/users/me

============================================
💾 СКАЧИВАНИЕ ПРОЕКТА
============================================

Весь код в: /workspaces/rey/

Архивировать:
  cd /workspaces
  tar -czf rey-project.tar.gz rey/

Git:
  cd /workspaces/rey
  git init
  git add .
  git commit -m "Initial"

============================================
📞 ПОМОЩЬ
============================================

Полная инструкция:
  INSTALLATION_GUIDE.md

Примеры API:
  API_EXAMPLES.md

Проверка окружения:
  bash check-environment.sh

Проверка кода:
  CODE_REVIEW.md

============================================

✨ ВСЁ ГОТОВО К ЗАПУСКУ!

Начни с команды:
  mongod

Потом в другом терминале:
  cd /workspaces/rey && npm run dev

И в третьем:
  cd /workspaces/rey/client && npm start

Удачи! 🚀

============================================
